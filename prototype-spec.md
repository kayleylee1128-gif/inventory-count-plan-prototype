# 盘点计划模块原型说明（行业标准条件优化版）

## 原型任务单

| 项目 | 内容 |
|---|---|
| 页面清单 | 盘点计划列表、生成记录视图、新建/编辑计划抽屉、手动执行确认弹窗 |
| 页面主任务 | 基于行业通用策略模板配置并管理盘点计划；查看每次计划生成结果 |
| 明确要求 | 缺货 SKU+货位、动销 SKU+货位、过去 X 天、剔除近期已盘点、每任务 SKU 明细数、每天/每周/每月、手动执行 |
| 明确不做 | 盘点任务执行、人员分配、差异复核、真实接口 |
| 保守假设 | “近期已盘点”按 SKU+货位且盘点任务已完成判断；命中明细按每任务上限自动拆分 |

## 行业条件模型

- 拣货异常：过去 X 天拣货缺货、缺货次数。
- ABC / 动销：ABC 分类、出库数量、统计周期。
- 抽盘：从过去 X 天已完成盘点的数据池中，按盘平、盘盈、盘亏结果范围随机抽取指定数量 SKU。
- 周期性盘点：按指定仓库通道或指定货位范围，周期生成盘点任务。
- 历史差异：差异率、连续差异次数、统计周期。
- 通用范围：仓库、SKU + 货位盘点维度、任务优先级。
- 通用排除：近期已完成盘点、已有待执行/执行中任务、零库存货位。
- 生成控制：每任务明细上限、单次生成上限、仓库/库区拆分、空货位策略。
- 执行模式：定时、仅手动、条件触发后立即执行。

依据：Microsoft Dynamics 365 周期盘点计划/阈值、SAP ABC Cycle Counting、Infor WMS Item + Location 盘点粒度。

## 已实现

- 计划列表：名称、触发依据、数据范围、拆分规则、周期、状态、最近执行及操作。
- 触发规则：拣货异常、ABC/动销、库存阈值、长期未盘、历史差异五类策略模板。
- 数据口径：统计过去 X 天，并剔除过去 X 天内完成盘点的 SKU+货位。
- 任务拆分：配置每个盘点任务的 SKU 明细数量，范围 1–1000。
- 执行周期：每天、每周、每月 X 号；支持关闭定时、仅保留手动执行。
- 手动执行：二次确认并强调近期已盘数据不会重复进入。
- 生成记录：展示执行方式、命中数、剔除数、任务数和结果。
- 交互：新建/编辑抽屉、规则实时预览、搜索、启停、手动执行反馈。

## 组件映射

- `ErpShell`：后台壳层与库存管理导航。
- `PageHeaderBar`：标题及新建主操作。
- `QueryFilterBar`：计划与记录筛选。
- `DataTablePanel`：计划与生成记录表格。
- `EditDrawer`：复杂规则配置。
- `RiskConfirm`：立即执行二次确认。
- `FeedbackState`：操作成功反馈。
- 周期切换与规则卡片使用 Ant Design 基线临时组合。

## 待确认口径

- ABC 分类是读取商品主数据，还是由计划按动销/价值实时计算。
- 剔除判断是否仅认“已完成”盘点，还是生成过/执行中任务也需要剔除。
- SKU 明细数量是“SKU 去重数”还是“SKU+货位明细行数”；当前按 SKU+货位明细行拆分。
- 月度执行遇到不存在的日期（如 31 号）时的顺延/提前策略。

## 交付级别

本轮按用户要求完成结构与视觉优化，使用项目 UI 组件契约和中文 B 端 ERP 视觉基线；不包含 Figma 或在线发布。
